components:
  ad:
    resources:
      limits:
        cpu: "150m"
      requests:
        cpu: "100m"
  checkout:
    resources:
      requests:
        cpu: 100m
  frontend-proxy:
    enabled: true
    service:
      type: LoadBalancer
  cart:
    resources:
      limits:
        memory: 160Mi
  accounting:
    resources:
      requests:
        cpu: "100m"
        memory: "100Mi"
      limits:
        cpu: "200m"
        memory: "120Mi"
  flagd:
    enabled: true
  llm:
    enabled: false
prometheus:
  enabled: false
grafana:
  enabled: false
jaeger:
  enabled: false
opentelemetry-collector:
  enabled: true
opensearch:
  enabled: false
