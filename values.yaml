components:
  flagd:
    enabled: true
    envOverrides: []
    config:
      create: true
      data:
        demo.flagd.json: |
          {
            "flags": {
              "adFailure": {
                "defaultVariant": "off",
                "state": "ENABLED",
                "variants": { "off": false, "on": true }
              },
              "adServiceFailure": {
                "defaultVariant": "off",
                "state": "ENABLED",
                "variants": { "off": false, "on": true }
              }
            }
          }
  frontend-proxy:
    enabled: true
    service:
      type: LoadBalancer
  cart:
    resources:
      limits:
        memory: 160Mi
  llm:
    enabled: false

grafana:
  enabled: false
jaeger:
  enabled: false
opentelemetry-collector:
  enabled: true
opensearch:
  enabled: false
